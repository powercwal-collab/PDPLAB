# PDP Lab 项目版本手册

> 更新时间：2026-07-14  
> 项目目录：`/Users/lixiao/Documents/人设背景提示词/outputs/pdp-lab-prototype`  
> Figma：[PDP Lab 完整 Flow](https://www.figma.com/design/4r1KuSyBLefGKULt2oFiU6/Untitled?node-id=0-1)

本文件用于让后续 Agent 快速理解项目、区分 1.0 与 2.0、恢复指定版本，并继续完成设计或开发。不要在没有读取本文件和 `AGENTS.md` 的情况下大范围重构。

## 当前激活版本：3.12（生产部署改造版）

3.12 在 3.11 已验收交互上进行兼容式生产改造，不改变评分规则、页面信息架构或本地开发入口：

- Django 通过 `PDP_ENV` 区分开发与生产；生产强制使用独立密钥、合法 Host/Origin、PostgreSQL、Redis 和安全 Cookie。
- 写接口恢复 CSRF 保护，前端统一获取并发送 CSRF Token；新增登录失败限流、生产密码校验和数据库/缓存 readiness。
- 新增 Gunicorn、Celery、Docker Compose、Nginx、TLS 覆盖配置、阿里云 OSS 可选存储与 Ubuntu 初始化/部署/回滚脚本。
- 新增 GitHub CI 与需人工触发的 ECS 发布工作流；release 按 commit 保存，健康检查通过后才切换 `current`。
- 目标 ECS 基线为 Ubuntu 22.04、2 vCPU / 8 GiB、80 GiB ESSD、5 Mbps；默认 Gunicorn 3 workers × 2 threads、Celery concurrency 2。
- 完整部署逻辑、生产变量、RDS/OSS 选择、HTTPS、验收与回滚见 `docs/PRODUCTION_DEPLOYMENT.md`。

验证状态：Django 开发检查通过、生产 `check --deploy` 通过（SimpleUI 的 X-Frame 检查由 Nginx `SAMEORIGIN` 明确接管）、21/21 后端测试通过、Vite 生产构建通过且首包已拆分、Compose/Actions YAML 与部署脚本语法通过；本地 `/api/health/ready/` 已实际返回数据库与缓存均可用。Docker 镜像构建需在已安装 Docker 的 ECS/CI 继续验证。

恢复方式：Git 标签 `pdp-lab-v3.12` 指向本版本；执行 `git switch -c restore-v3.12 pdp-lab-v3.12` 可创建恢复分支。上一稳定交互版本继续由 `pdp-lab-v3.11` 保留。

## 历史版本：3.11（统一下拉弹层关闭逻辑版）

3.11 在 3.10 响应式与账号隔离基线上统一轻量下拉弹层的关闭行为，不改变页面结构、业务数据或评分规则：

- 新增共享 `useDismissiblePopover`，统一处理点击框体外区域关闭与 `Escape` 关闭，避免账号菜单、项目菜单和上传项目选择各自维护不同监听器。
- 首页右上角账号菜单、工作台侧栏账号菜单、工作台项目切换器、上传页项目选择器均接入同一关闭契约；点击另一个触发器时，已打开的同类弹层会先关闭。
- 触发按钮补齐 `aria-haspopup="menu"`、`aria-expanded`，菜单补齐 `role="menu"` 与菜单项语义，保留既有视觉、菜单内部滚动和固定操作项。
- 原生选择控件继续使用浏览器原生关闭规则；确认弹窗继续使用遮罩和弹窗专属关闭规则，不与轻量下拉菜单混用。

主要改动文件：`src/App.jsx`、`src/styles.css`、`AGENTS.md`、`agent.md`、`design-qa.md`。

验证状态：Vite 生产构建通过；Django diagnosis 19/19 测试通过；首页账号、侧栏账号、工作台项目切换和上传项目选择四类菜单均通过点击外部关闭与 Escape 关闭测试；刷新后的浏览器控制台无新增错误或警告。验收截图为 `design-qa-dropdown-account-open.png`、`design-qa-dropdown-sidebar-account-open.png`、`design-qa-dropdown-project-open.png`、`design-qa-dropdown-upload-project-open.png`。

恢复方式：Git 标签 `pdp-lab-v3.11` 指向本版本；执行 `git switch -c restore-v3.11 pdp-lab-v3.11` 可创建恢复分支。上一版本继续由 `pdp-lab-v3.10` 保留。

## 历史版本：3.10（响应式布局与评分动效版）

3.10 在 3.9 上传交互验收版上补齐手机和平板适配，并保持桌面信息架构、评分规则和后端数据不变：

- 工作台在 1024px 以下改为顶部横向导航；900px 以下仪表盘改为单列；640px 以下进一步压缩项目工具栏、卡片、上传页、评分历史、账户弹窗和设置页。
- 首页最近项目在 iPad 使用双列、iPhone 使用单列；工作台所有主卡在 iPhone / iPad 均无整页横向溢出。
- 手机评分卡的 3 星与 7 星端点增加安全边距，五个星级名称完整保留；“为什么还不是”提示条恢复 14px 左右内边距，不再贴边。
- 总分从 0 滚动到当前实际分数；星级结论轻量淡入，刻度线由左向右填充，圆形标记移动到实际星级并弹性收束。
- 动效在切换项目或评分版本时重新播放，并在系统启用 `prefers-reduced-motion` 时直接显示最终状态。
- 注册后的新账号项目列表为空；项目创建自动绑定当前用户，项目列表、上传、评分与任务访问都只允许项目 owner，历史无主项目不再作为所有账号的共享初始化内容。
- 数据迁移 `0012_claim_legacy_projects` 会在既有环境中把无主历史项目一次性归属给 `powercwal`（若不存在则使用首个超级管理员），保留当前管理员的本地项目历史；全新环境没有管理员时仍保持无共享项目的初始化状态。

主要改动文件：`src/App.jsx`、`src/dashboard-overrides.css`、`src/responsive.css`、`AGENTS.md`、`agent.md`、`design-qa.md`。

验证状态：Vite 生产构建通过；Django diagnosis 19/19 测试通过；浏览器控制台无错误。iPhone 390 × 844 与 iPad 820 × 1180 实测 `documentWidth === viewportWidth`；手机五个星级标签均完整位于评分卡内，提示条左右内边距均为 14px。数值滚动在 250ms 时显示中间值、完成后落到 89.5，刻度填充与标记动画均已挂载。账号隔离测试确认新账号初始列表为空、他人项目不可上传、创建项目自动写入当前 owner。

恢复方式：Git 标签 `pdp-lab-v3.10` 指向本版本；执行 `git switch -c restore-v3.10 pdp-lab-v3.10` 可创建恢复分支。上一版本继续由 `pdp-lab-v3.9` 保留。

## 历史版本：3.9（上传卡片对齐与统一项目菜单版）

3.9 在 3.8 已验收的评分闭环上修正上传前置页，不改变上传接口、项目数据或诊断任务逻辑：

- “选择项目”和“上传内容”两张卡片使用相同的 330px 内容轴，步骤标签、主标题与底部控件的左右边界完全一致。
- 两张卡片的步骤标签、标题和控件分别处于相同纵坐标；内容组相对卡片中心偏差均为 0px。
- 项目选择框与文件拖入框统一为 64px 高；主操作文字统一为 12px、700 字重和同一正文色。
- 上传页项目选择改为自定义菜单，并与工作台项目切换器共用 `ProjectMenuPanel`：完整项目列表、选中勾选、固定“新建诊断项目”、内部滚动、点击外部关闭和 Escape 关闭逻辑一致。

主要改动文件：`src/App.jsx`、`src/styles.css`、`AGENTS.md`、`agent.md`、`design-qa.md`。

验证状态：Django 18/18 测试通过；Vite 生产构建通过；浏览器控制台无错误。两张卡片实测标签均为 `y=292 / h=16`、标题均为 `y=317 / h=25`、控件均为 `y=368 / h=64 / w=330`；菜单包含 7 个当前项目、选中态带勾选，点击外部和 Escape 均可关闭。浏览器证据为 `design-qa-upload-cards-aligned.png` 与 `design-qa-upload-project-menu.png`。

恢复方式：Git 标签 `pdp-lab-v3.9` 指向本版本；执行 `git switch -c restore-v3.9 pdp-lab-v3.9` 可创建恢复分支。上一版本由 `pdp-lab-v3.8` 保留。

## 历史版本：3.8（评分历史管理与版本联动版）

3.8 在 3.7 已验收的评分工作台上补齐评分记录管理，不改变服务端评分规则或既有版本内容：

- 左侧评分版本列表固定为与右侧 11 模块明细卡一致的 653px 高度；超出约 7 条记录后在列表内部纵向滚动，不再向下撑高页面。
- 每条记录使用独立右侧信息列：分数固定右上，删除图标固定右下，图标底边与左侧星级及评分方式文案底边对齐。
- 删除图标打开不可恢复操作确认弹窗；支持取消、点击遮罩或 Escape 关闭、删除中禁用、后端错误反馈和成功提示。
- Django 新增受登录与项目访问范围保护的 `DELETE /api/diagnoses/<id>/`；删除当前版本后回退到第一条剩余记录，删除其他版本时保持当前选择。
- 评分记录中选中的版本成为工作台当前诊断上下文；项目总览、评分诊断、得分缺口和优化路线均跟随该版本更新。
- 右侧模块明细保留固定摘要与表头，11 个模块在卡片内部滚动；表头与内容共用同一列网格，并增加真实证据缩略图列。
- 表格缩略图保持 `48 × 36px` 紧凑裁切；点击后仅在弹窗中按原始宽高比展示完整证据图，超长内容在弹窗图片区内部纵向滚动。

主要改动文件：`src/WorkflowPages.jsx`、`src/App.jsx`、`src/styles.css`、`src/services/api.js`、`backend/diagnosis/views.py`、`backend/diagnosis/urls.py`、`backend/diagnosis/tests.py`、`AGENTS.md`、`design-qa.md`。

验证状态：Django 18/18 测试通过；Vite 生产构建通过；1093 × 898 浏览器验收通过。评分记录表格、证据缩略图和弹窗均已实际渲染；弹窗图片区 `clientHeight = 520px`、完整长图 `scrollHeight = 29253px`，图片保持 `object-fit: contain`，表格缩略图保持 `object-fit: cover`。用户已确认弹窗滑动正常。浏览器证据为 `design-qa-score-history-evidence.png` 与 `design-qa-score-history-evidence-modal.png`。

恢复方式：Git 标签 `pdp-lab-v3.8` 指向该版本，`pdp-lab-v3.7` 继续保留其修改前的完整验收版本。

## 历史版本：3.7（模块评分轮播补全版）

3.7 在 3.6 已验收的评分工作台上补全项目总览的 11 模块展示，不改变评分数据、排序规则或卡片尺寸：

- 模块成熟度卡仍保持每页最多 6 行和原有行密度；11 个模块拆为 `6 + 5` 两页。
- 卡片底部增加两枚分页圆点；活动页使用蓝色长圆点，另一页使用灰色圆点，点击即可双向切换。
- 圆点使用真实按钮语义、页码标签、键盘焦点和 `aria-current`；切换动画遵循系统“减少动态效果”设置。
- 当项目诊断数据变化时自动回到第 1 页，避免切换项目后停留在尾页。
- 浏览器实测两页合计 11 个模块；模块卡两页均为 `clientHeight = scrollHeight = 388px`，没有纵向或横向溢出。

主要改动文件：`src/App.jsx`、`src/dashboard-overrides.css`、`AGENTS.md`、`design-qa.md`。

验证状态：分页圆点双向切换通过；6 + 5 行完整性通过；控制台无错误；Vite 生产构建通过。浏览器证据为 `design-qa-module-carousel-page-1.png` 与 `design-qa-module-carousel-page-2.png`。

## 历史版本：3.6（评分界面与证据展示验收版）

3.6 在 3.5 的真实评分链路上完成了前端最终验收，未改变评分规则、模型配置或已保存数据：

- 项目切换器读取后端完整项目列表；单屏最多展示 20 个项目，超过后在菜单内部滚动，“新建诊断项目”固定在列表外。
- 项目菜单支持点击外部区域和按 `Escape` 关闭，避免遮挡工作台。
- 项目总览评分卡保持左侧总分、右侧星级结论与下方五段星级轴；删除无效的“当前 T1-minus”信息。
- 评分文案区与星级浮标间距固定为 16px；原因说明完整展示，卡片与说明区均无纵向溢出，并保留 46px 底部空间。
- “当前 X 星”标签默认以圆形刻度点为水平中心；6.5 星状态实测中心偏差为 0px，只有满 7 星时启用右边界保护。
- 评分诊断右侧详情改为读取每个模块自己的判断、证据、OCR 摘要、成熟度逻辑和规则标准；弱、中、强状态不再复用同一组静态文案。
- 强模块展示绿色“保持强项”，弱/中模块展示对应补强建议；内容与 11 模块评分、证据记录和运行时规则一致。
- 项目总览“前三大得分缺口”不再使用静态示例：按 `模块满分 - 当前得分` 计算，过滤零缺口，按可提升分值、成熟度和规则顺序排序，并绑定对应模块判断/证据。
- “为什么还不是 X 星”、P0 任务和完整优化路线都读取同一组动态缺口；Nike 与迪桑特切换后模块、文案、提升值和预计总分保持一致。
- 全部项目卡已逐项核对：历史 `4.9 星` 按当前评分区间归一化为 `5 星`；无上传源的持久化项目只显示文件夹占位，不再借用示例封面；更新时间取项目、最新诊断和封面源中的最新值。

主要改动文件：`src/App.jsx`、`src/WorkflowPages.jsx`、`src/dashboard-overrides.css`、`backend/diagnosis/views.py`、`backend/diagnosis/tests.py`、`AGENTS.md`、`design-qa.md`。

Figma 状态：本轮为本地实现验收，未修改 Figma 节点；设计源仍为本文顶部链接及当前任务的浏览器批注截图。

验证状态：用户已于 2026-07-14 确认既有界面 OK；1093 × 898 目标视口下，评分卡、项目菜单、弱/中/强诊断模块、Nike/迪桑特动态缺口与 P0 路线、7 个项目卡片通过；6.5 星标签与圆形刻度点中心偏差为 0px；新页面运行无控制台错误；Django 17/17 测试和 Vite 生产构建通过。完整记录见 `design-qa.md`。

恢复方式：如需回退本轮视觉与交互调整，优先按 Git 历史还原上述文件；不要删除 3.5 的模型配置、评分版本和证据数据。若仅恢复旧评分卡，不应回退后端证据序列化修复。

未完成项：品牌资产库和 AI 生图仍按既定范围保持灰色不可用；正式部署前仍需配置生产数据库、任务队列、对象存储、HTTPS、密钥管理和监控。

## 历史版本：3.5（MiMo 真实模型评分）

3.5 在 3.4 的安全边界上完成了 ModelVerse MiMo V2.5 接入：

- AI 模型后台新增 `responses / chat_completions` 独立协议设置；PDP Skill 设置仍保持独立入口。
- 当前启用 ModelVerse OpenAI 兼容接口、`mimo-v2.5` 与 `chat_completions`，API Key 加密保存在后端且不返回前端。
- Chat Completions 使用图片 Data URL 分析 PNG/JPG；PDF 会明确阻断，避免误走不兼容的 Files API。
- 真实 Nike 长图通过 11 模块、11 证据、离散系数与服务端计分契约，验证结果 89.5 分、6.5 星；该次为不落库验证，不覆盖现有 Codex 复核版本。
- Mock 默认阻断；模型输出字段不合法、证据缺失或模块不完整时任务失败并弹窗，不做猜测性修复或 Mock 回退。

## 历史版本：3.4（真实项目展示与失败反馈）

3.4 在统一规则配置上补齐了真实项目验证和接入边界：

- 首页项目卡读取后端真实评分、星级和上传原图封面；进入工作台后读取同一评分版本。
- 已用 Nike Vomero 18 长图创建本地验证项目，Codex 按 PDP Skill 复核为 87 分、6.5 星，并以 `codex_verified` 独立记录。
- 上传建任务失败及异步模型/Skill 执行失败均增加阻断弹窗，不再只显示行内错误。
- 项目总览星级轴改为 3–7 星五个核心段位，按星级定位，并显示对应页面类型。
- 明确禁止复用本地 Codex/ChatGPT 登录凭证作为网站 API Key；真实模型调用必须使用独立配置的服务端 API Key。

## 历史版本：3.3（规则与集成配置统一）

3.3 在 3.2 的异步诊断基础上统一后台、运行时与前端规则来源：

- Django 管理后台新增相互独立的“AI 模型 API 设置”和“PDP Skill 接入设置”；分别管理模型/API Key 与远程 Skill URL/端口/Token。
- 当前 PDP 规则已与本地 `pdp-detail-page-methodology` 评分标准校验，记录规则 SHA-256 修订号。
- 前端项目总览、评分诊断、人工修订和设置页统一读取 `/api/diagnosis-config/` 的 11 模块与 13 个星级区间。
- 当时无 OpenAI Key 会回退 mock、配置 Key 后切换 Responses API；该旧行为已在 3.5 改为 Mock 默认阻断与可选协议。
- 品牌资产与 AI 生图仍按既定范围保持灰色不可用状态。

## 历史版本：3.2（异步 AI 诊断）

3.2 在评价闭环上接入了真实异步任务、证据结构和可替换的模型适配器：

- 上传后通过 `DiagnosisJob` 建任务，Celery Worker 执行 parsing / slicing_ocr / module_mapping / scoring。
- `PageEvidence` 保存模块、页码、归一化 bbox、OCR 文字、模型解释与置信度。
- `ModuleAssessment` 保存 11 个模块的权重、系数、得分、成熟度和判断。
- 模型只提供 `0 / 0.5 / 1` 系数和证据；后端基于版本化 `ScoringStandard` 计算总分和 1~7 星。
- AI 结果在 11 模块与证据完整通过后自动锁定，`confirmation_mode=ai_auto`，无需人工确认。
- 人工评分页保留为可选修订入口，修订会创建新版本，不覆盖 AI 原始版本。
- 已实现 `openai` 适配器：通过 Responses API 结构化输出分析 PNG/JPG/PDF，并在请求后删除远程临时文件。
- 3.2 当时的 `auto` 模式会在无 Key 时使用 mock；该旧行为已被 3.5 的 Mock 安全阻断取代。

3.2 主链路：

```text
登录 / 注册 → 首页 → 选择项目并上传 PDP → 异步诊断进度
  → 证据与 11 模块结构化输出 → 服务端计分 → AI 版本自动锁定
  → 评分记录 / 项目总览 →（可选）人工修订新版本
```

实现报告：`docs/ASYNC_DIAGNOSIS_OPENAI_REPORT.md`。

## 历史版本：3.1（评价优先）

3.1 将开发范围收紧到可用的评价闭环，并保留账号与项目基础能力：

- 评分确认支持逐模块选择“弱 / 中 / 强”，自动计算模块得分、总分与整体星级。
- 未完成 11 个模块确认时禁止锁定；后端再次校验完整性、成熟度、得分范围和总分一致性。
- Django 新增 `DiagnosisVersion`，每次锁定自动递增版本并保存操作者、时间和完整模块 JSON。
- 工作台新增“评分记录”，可查看历史版本和 11 项模块明细。
- 项目总览与评分诊断会读取最新锁定版本，不再只展示固定 mock 分数。
- 登录、注册、会话恢复、个人资料、通知偏好、项目创建和 PDP 文件上传继续使用真实 API。
- 品牌资产与 AI 创作改为灰色不可用状态，明确标记“暂未开放”。
- 验证报告：`docs/EVALUATION_FIRST_COMPLETION_REPORT.md`。

3.1 主链路：

```text
登录 / 注册 → 首页 → 选择项目或上传 PDP → 诊断进度
  → 评分确认（11 项完整校验）→ 锁定评分版本 → 评分记录 → 项目总览
```

当时限制：诊断进度和 AI 初评证据为本地模拟内容，尚未接 OCR/视觉分析服务；资产库与 AI 生成已主动停用。

## 历史版本：3.0

3.0 在 1.1 视觉与交互基线之上增加 Django 后端基础设施，前端页面暂时仍使用 mock 数据，后续按 API 逐步替换。

- Python：项目独立 `.venv`，Python 3.12.13。
- Django：5.2.16。
- 后端目录：`backend/`。
- Django 工程：`pdp_lab_backend`。
- 首个业务应用：`diagnosis`。
- 首个数据模型：`Project`。
- 基础 API：`GET /api/health/`、`GET /api/projects/`。
- Vite 开发代理：`/api` → `http://127.0.0.1:8000`。
- 本地数据库：SQLite，仅用于开发验证并被 Git 忽略。

启动命令：

```bash
npm run dev
npm run dev:api
```

当前后端仅建立项目与接口骨架；上传、诊断、品牌资产匹配和生成任务仍需在下一阶段实现。

3.0 账号能力补充：

- 产品入口前置登录/注册页。
- Django 提供 `/api/auth/login/`、`/api/auth/register/`、`/api/auth/logout/`。
- 左下角用户卡打开账号菜单，可进入个人主页、通知设置、安全与登录、Django 管理后台或退出登录。
- 账户管理页包含资料编辑、账号角色、设备状态、通知偏好与后台入口。

当前本地代码以 1.0 的紧凑首页与 Inter 字体为视觉基线，并新增独立“诊断进度”页。2.0 的思源字体与大版首页规范仍保留在本文件中，便于之后恢复，但不是当前本地激活版本。

1.1 主链路：

```text
首页 → 导入 PDP → 诊断进度 → 评分确认 → 项目总览
```

1.1 已完成：

- `src/App.jsx` 增加独立状态“诊断进度”。
- 上传完成后不直接进入工作台，先进入独立诊断进度页。
- `src/WorkflowPages.jsx` 的分析页面增加独立页头、百分比、四阶段任务状态和返回首页。
- 首页恢复 1.0 的 920px Hero、118px 上传区、220px 项目卡和 Inter 优先字体。
- 生产构建通过。
- 1440 × 1000 浏览器验证通过，无横向溢出和控制台错误。
- 已验证交互：上传 → 诊断进度 → 评分确认 → 项目总览。

完整 Flow 与部署确认稿：`docs/FLOW_DEPLOYMENT.md`。

## 1. 项目目标

PDP Lab 是一个轻量 PDP 诊断、评分与优化工作台。使用者上传商品详情页后，可以快速理解评分内容、问题依据和后续优化方向，并继续完成：

1. PDP 内容上传与解析。
2. 11 个模块评分及整体星级判断。
3. P0 / P1 / P2 优化路线生成。
4. 品牌资产库匹配。
5. AI 优化版本生成。
6. 优化前后页面对比。
7. 复评分与下一轮优化。

未来正式版本需要能够直接接入品牌 DAM/资产库，并将已选品牌资产传给图像或页面生成服务。

## 2. 当前技术结构

- 前端：React 19 + Vite 6。
- 图标：`@phosphor-icons/react`。
- 图表：Recharts。
- 当前交互状态：前端内存状态与真实 mock 数据，无正式后端持久化。
- 入口文件：`src/App.jsx`。
- 工作流页面：`src/WorkflowPages.jsx`。
- 主样式：`src/styles.css`。
- 首页与工作台切换补充样式：`src/home-overrides.css`。
- 项目图片：`public/projects/`。
- 最终优化素材：`public/final/`。

常用命令：

```bash
npm run dev -- --host 0.0.0.0 --port 4173 --strictPort
npm run build
```

## 3. 不随版本回退的核心交互

以下逻辑在 1.0 和 2.0 中都应保留：

- 首页独立于项目工作台，不显示工作台侧边栏。
- 选择最近项目后进入“项目总览”。
- 上传完成后进入独立“诊断进度”，再进入“评分确认”。
- 工作台顶部始终提供“返回首页”。
- 工作台顶部提供快速项目切换器。
- 整体 PDP 使用星级；单个评分模块只使用“弱 / 中 / 强”。
- 主链路：

```text
首页
  ├─ 选择最近项目 ───────────────→ 项目总览
  └─ 上传/新建项目 → 诊断进度 → 评分确认 → 项目总览
                                            ↓
诊断详情 → 优化路线 → 品牌资产匹配 → AI 生成
                                            ↓
最终优化页面 → 复评分 → 项目总览 / 下一轮优化
```

## 4. 1.0 版本：初始完整工作台

### 4.1 版本定义

1.0 指完整业务 Flow 已建立，但尚未按最新参考图扩大首页层级、也未完成全项目思源字体迁移的版本。

### 4.2 视觉特征

- 全局字体：`Inter` 为主，`Noto Sans SC` 和 `PingFang SC` 为回退。
- 首页 Hero 最大宽度约 920px。
- 首页主标题约 31px。
- 上传区高度约 118px，圆角约 18px。
- 最近项目内容最大宽度约 1180px。
- 新建项目卡高度约 220px，项目封面高度约 170px。
- 首页信息密度更紧凑，适合早期功能验证。
- Figma 旧首页根节点曾为 `5:2`，已在 2.0 重建时删除；需要通过 Figma 版本历史恢复，不要伪造该节点 ID。

### 4.3 1.0 字体配置

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Sans+SC:wght@400;500;600;700&display=swap');

:root {
  font-family: Inter, 'Noto Sans SC', 'PingFang SC', sans-serif;
}
```

### 4.4 还原 1.0

当用户说“还原 1.0”时：

1. 保留 `App.jsx` 和 `WorkflowPages.jsx` 的完整业务链路。
2. 将全局字体恢复为 4.3 的字体配置。
3. 将首页恢复为以下关键尺寸：
   - `.home-hero`: `max-width: 920px; padding: 68px 0 55px`。
   - `.home-hero h1`: `font-size: 31px`。
   - `.home-upload`: `min-height: 118px; border-radius: 18px; padding: 22px 24px`。
   - `.recent-projects`: `max-width: 1180px`。
   - `.project-grid`: 4 列、`gap: 16px`。
   - `.new-project`: `height: 220px`。
   - `.project-cover`: `height: 170px`。
4. Figma 需要使用文件版本历史恢复到 2026-07-12 思源字体迁移之前的版本；不要只修改当前节点名称来冒充 1.0。
5. 执行 `npm run build`，并重新检查首页、项目总览和上传页。

## 5. 2.0 版本：思源字体与参考图首页

### 5.1 版本定义

2.0 指保留 1.0 完整业务 Flow，同时统一思源字体、按用户提供的首页参考图重新布局，并进行全文件溢出与对齐检查的版本。

### 5.2 已完成内容

- 本地全局字体改为：

```css
font-family: 'Noto Sans SC', 'Source Han Sans SC', '思源黑体', 'PingFang SC', sans-serif;
```

- Google Fonts 只加载 Noto Sans SC 的 400 / 500 / 600 / 700 / 900。
- 首页取消外层蓝灰背景、圆角应用壳和工作台侧栏，使用完整白色画布。
- 首页结构调整为：居中品牌标识、大标题、副标题、全宽上传区、四项能力标签、最近项目四列卡片。
- 首页项目卡调用 `public/projects/` 中的真实长图素材。
- 本地响应式断点：1100px 和 820px。
- `npm run build` 已通过。
- Figma 全文件的文字样式和组件主件已迁移到 `Noto Sans SC`。
- Figma 首页已按参考图重建，当前根节点为 `20:16`。
- Figma 首页项目封面节点：`20:50`、`20:55`、`20:60`。
- Figma 的 16 个页面已完成自动检查：无根画布溢出、无裁切文本、无 Inter 残留、无缺失字体。
- “诊断处理中”页面的两个空白零尺寸文本层 `5:123`、`5:128` 已删除。

### 5.3 Figma 字重映射

| 1.0 | 2.0 |
|---|---|
| Inter Bold | Noto Sans SC Bold |
| Inter Semi Bold | Noto Sans SC Bold |
| Inter Medium | Noto Sans SC Medium |
| Inter Regular | Noto Sans SC Regular |
| 首页主标题 | Noto Sans SC Black |

### 5.4 当前验证状态

已验证：

- 前端生产构建成功。
- Figma 字体、根画布溢出、文本裁切、缺失字体和零尺寸异常层检查通过。
- Figma 首页、评分确认、项目总览和最终优化页已生成截图并人工检查。

仍需验证：

- 本地浏览器自动化受到当前 Browser URL 安全策略阻断，不能把“构建通过”写成“浏览器视觉验证通过”。
- 下次具备可用本地浏览器连接时，需要重新完成首页、上传、项目总览、项目切换和完整主流程点击测试。
- Figma 使用超长 PDP 截图作为项目封面，当前采用裁切填充；如继续精修，应创建顶部区域专用缩略图，避免使用长图中段作为封面。

### 5.5 恢复或继续 2.0

当用户说“恢复 2.0”或“继续 2.0”时：

1. 确认 `src/styles.css` 的全局字体仍为 Noto Sans SC / Source Han Sans SC。
2. 确认首页仍包含 `.home-wordmark`、`.home-upload`、`.home-tags`、`.project-grid`。
3. 确认 `home-overrides.css` 中 `.app-shell.home-mode` 为全宽、无边框、无圆角、无阴影。
4. 执行 `npm run build`。
5. 检查 1440px、1100px、820px 三个宽度，优先修复横向滚动、文字换行、按钮挤压和卡片错位。
6. 对照 Figma 首页节点 `20:16` 和用户参考图完成视觉 QA。

## 6. 1.0 与 2.0 对比

| 项目 | 1.0 | 2.0 |
|---|---|---|
| 业务 Flow | 完整 | 完整，保持不变 |
| 全局字体 | Inter 优先 | 思源字体 / Noto Sans SC 优先 |
| 首页定位 | 紧凑功能入口 | 品牌化独立入口 |
| 首页标题 | 31px | 46px，900 字重 |
| 上传区 | 118px 高 | 190px 高、全宽 |
| 最近项目卡 | 220px / 170px 封面 | 340px / 272px 封面 |
| 外层应用壳 | 保留应用壳视觉 | 首页全白、无壳；工作台保留应用壳 |
| Figma 首页 | 旧节点 `5:2` | 当前节点 `20:16` |
| Figma 字体 | Inter | Noto Sans SC |
| 自动布局检查 | 初次检查通过 | 字体迁移后重新检查通过 |
| 本地浏览器 QA | 已做过早期交互验证 | 当前环境仍待重新验证 |

## 7. 前后端适配约定

建议正式实现时保持以下对象边界：

- `Project`：项目名称、品牌、品类、最近版本、更新时间。
- `PdpSource`：图片、PDF、URL、上传状态、解析状态。
- `Diagnosis`：总分、星级、成熟度、11 模块结果、证据。
- `OptimizationTask`：优先级、模块、预计增益、状态、依赖资产。
- `BrandAsset`：DAM ID、标签、版权、匹配度、缩略图、原文件。
- `GenerationJob`：输入任务、资产 ID、Prompt、状态、多个版本。
- `OptimizedVersion`：生成结果、变更说明、复评分、版本关系。

品牌资产库接入时，前端不要直接依赖供应商返回结构；由后端建立统一的 `BrandAsset` 适配层。

## 8. 修改边界

- 不要因为恢复首页而删除工作台 Flow。
- 不要把整体星级用于单模块评分。
- 不要让首页重新出现工作台侧边栏。
- 不要删除“返回首页”和“快速切换项目”。
- 不要用静态占位替换已经存在的真实项目图片。
- 不要声明浏览器 QA 通过，除非实际打开页面、检查控制台并完成主流程交互。
- 修改 Figma 字体时，必须先确认字体可用并加载对应字重。

## 9. 可直接调用的指令

```text
读取 agent.md 和 AGENTS.md，按 1.0 基线还原字体与首页布局，保留所有 2.0 业务功能。
```

```text
读取 agent.md，恢复 2.0 视觉规范并检查 1440 / 1100 / 820 三个断点。
```

```text
读取 agent.md，继续 2.0 QA：检查所有页面溢出、错误对齐、字体残留和主流程交互。
```

```text
读取 agent.md，以 2.0 为基础接入品牌资产库 API，不改变诊断与优化任务的数据边界。
```

## 10. 版本更新规则

- 视觉与字体小修只更新 2.0 的“当前验证状态”。
- 业务 Flow、数据结构或页面层级发生明显变化时，新增 3.0，不要覆盖 1.0 / 2.0 的定义。
- 每次新增版本必须记录：目标、改动文件、Figma 节点、验证状态、恢复方式和未完成项。
- `agent.md` 是版本与恢复手册；`AGENTS.md` 只保存短小、长期有效的项目规则。
- 1.1 是当前本地交互版本；不要误将 2.0 的字体与首页样式重新覆盖到本地，除非用户明确要求“恢复 2.0”。
